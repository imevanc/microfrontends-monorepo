{
  "projects": {
    "host": {
      "rootDirectory": "apps/host",
      "buildCommand": "npx turbo run build --filter=host",
      "outputDirectory": "dist"
    },
    "journeya": {
      "rootDirectory": "apps/journeya",
      "buildCommand": "npx turbo run build --filter=journeya",
      "outputDirectory": "dist"
    }
  }
}